import { PageHeader } from '@/components/page-header';
import { projects } from '@/lib/demo-data';
import { Plus } from 'lucide-react';
const money=(n:number)=>new Intl.NumberFormat('fr-FR',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n);
export default function Projects(){return <><PageHeader eyebrow="Objectifs" title="Projets & enveloppes" subtitle="Réservez une partie de vos entrées pour des projets sans confondre allocation et dépense réelle." actions={<button className="btn btn-primary"><Plus size={16}/>Nouveau projet</button>}/><div className="grid-3">{projects.map(p=><div className="card" key={p.name}><div className="section-title">{p.name}</div><div className="section-caption" style={{marginTop:5}}>Échéance {p.deadline}</div><div className="metric">{money(p.allocated)}</div><div className="section-caption">alloués sur {money(p.target)}</div><div className="progress" style={{marginTop:18}}><span style={{width:`${p.allocated/p.target*100}%`}}/></div><div className="kpi-row"><span>Déjà dépensé</span><strong>{money(p.spent)}</strong></div><div className="kpi-row"><span>Reste à financer</span><strong>{money(p.target-p.allocated)}</strong></div></div>)}</div></>}
